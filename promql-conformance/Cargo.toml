[package]
name = "promql-conformance"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish = false

[dependencies]
promql-engine = { path = "../promql-engine" }
promql-parser = { path = "../promql-parser" }
regex = { workspace = true }
serde = { workspace = true }
thiserror = { workspace = true }
toml = { workspace = true }

[dev-dependencies]
libtest-mimic = "0.8"

# The suite registers one test per corpus case at runtime, so it needs
# its own harness rather than #[test] attributes.
[[test]]
name = "promqltest"
harness = false
